# 邮智办第一阶段开发计划

## 一、第一阶段目标

第一阶段的目标不是直接完成“邮智办”全部功能，而是先证明三大模块都可以独立运行，并且前后端能够真正连通。

当前三条开发分支为：

```text
A：feature/frontend_initial
B：feature/backend_initial
C：feature/airag_initial

第一阶段结束时，希望达到：

A：Vue 能运行
B：FastAPI 能运行
C：AI/RAG 的输入输出和第一批资料准备完成

并且：

Vue → FastAPI → Vue

可以真正完成一次通信

## 二、成员 A：前端初始化

成员 A 第一阶段的主要目标：

把 Vue 前端项目跑起来，并能够调用后端测试接口。

主要负责目录
frontend/
第一阶段任务
创建 Vue 3 + Vite 项目。
确保项目可以正常安装依赖。
确保项目可以正常启动。
创建一个最简单的“邮智办”首页。
首页添加一个“测试后端连接”按钮。
通过 Axios 或 Fetch 请求 FastAPI 后端。
将后端返回的信息展示在页面上。
第一版页面可以非常简单

例如：

邮智办
北邮校园事务智能办理助手

[ 测试后端连接 ]

后端状态：未连接

点击按钮后，请求：

GET /api/health

如果后端正常，则页面显示：

✅ 邮智办后端运行正常
第一阶段目录可以先保持简单
frontend/
├── index.html
├── package.json
├── vite.config.js
└── src/
    ├── main.js
    └── App.vue
第一阶段暂时不要做
❌ 完整登录注册
❌ 用户画像
❌ 管理员页面
❌ 待办系统
❌ 查询历史
❌ RAG
❌ 复杂视觉设计
❌ 在前端编写业务规则
成员 A 完成标准
 npm install 可以正常执行
 npm run dev 可以正常执行
 浏览器可以正常打开 Vue 页面
 可以调用后端 /api/health
 可以在页面中显示后端返回结果

## 三、成员 B：后端初始化

成员 B 第一阶段的主要目标：

把 FastAPI 后端服务跑起来，并提供一个最简单的测试接口。

主要负责目录
backend/
第一阶段任务
创建 FastAPI 项目。
确保 Python 环境正常。
确保 FastAPI 服务可以启动。
创建 /api/health 测试接口。
返回简单 JSON。
与成员 A 完成第一次前后端联调。
第一阶段代码可以非常简单

例如：

from fastapi import FastAPI

app = FastAPI()

@app.get("/api/health")
def health():
    return {
        "success": True,
        "message": "邮智办后端运行正常"
    }

启动后访问：

http://127.0.0.1:8000/api/health

预计返回：

{
  "success": true,
  "message": "邮智办后端运行正常"
}

同时访问：

http://127.0.0.1:8000/docs

应该能够打开 FastAPI Swagger 页面。

第一阶段暂时不要做
❌ SQLite 完整数据库
❌ JWT
❌ 登录权限
❌ 管理员系统
❌ 用户画像 CRUD
❌ DeepSeek
❌ RAG
❌ 大量 services / models / schemas 文件
成员 B 完成标准
 Python 环境正常
 FastAPI 可以启动
 /api/health 可以访问
 /docs 可以访问
 Vue 能够成功请求 FastAPI
推荐 Commit
feat: initialize FastAPI backend

## 四、成员 C：AI / RAG 初始化

成员 C 第一阶段不要急着完成完整 RAG。

第一阶段的主要目标：

明确 AI/RAG 模块未来如何工作，并准备第一批真实北邮事务资料。

主要负责目录
airag/
第一阶段任务
1. 选择 3～5 个 Demo 事务

建议优先选择：

缓考申请
成绩复核
证明开具
奖学金申请
请假

不需要第一阶段就覆盖整个北邮。

2. 整理第一批真实资料

每个事务尽可能准备：

学校正式制度
学院通知
办事指南
相关补充说明

例如“缓考申请”可以准备：

本科生考试相关管理规定
学院缓考通知
具体办理说明
3. 初步确定 Metadata

每份资料以后建议至少考虑以下字段：

service
college
degree
grade
campus
source
updated_at
status

例如：

{
  "service": "缓考申请",
  "college": "玛丽女王海南学院",
  "degree": "本科",
  "campus": "海南",
  "source": "2026年缓考申请通知",
  "status": "active"
}
4. 与成员 B 约定输入输出

未来成员 B 可能向 C 传入：

{
  "question": "我想申请缓考",
  "profile": {
    "college": "玛丽女王海南学院",
    "degree": "本科",
    "campus": "海南"
  }
}

C 第一阶段至少应该明确未来返回什么结构，例如：

{
  "service": "缓考申请",
  "chunks": [],
  "sources": []
}

后续再逐步加入：

Intent Recognition
Query Rewrite
Embedding
Vector Search
Metadata Filtering
Context Builder
DeepSeek
Validator
第一阶段暂时不要求
❌ 完整向量数据库
❌ 完整 DeepSeek 调用
❌ 完整 Metadata Filtering
❌ 完整 Prompt 系统
❌ 完整智能问答流程
成员 C 完成标准
 明确第一批 Demo 事务
 准备第一批真实资料
 确定 Metadata 字段
 确定 B → C 输入格式
 确定 C → B 输出格式
 AI/RAG 目录可以继续扩展

## 五、第一阶段最关键任务：A + B 联调

第一阶段真正的核心不是三个人各自“写完自己的部分”，而是：

Vue 和 FastAPI 必须完成第一次真实通信。

数据流
浏览器
↓
Vue
↓
GET /api/health
↓
FastAPI
↓
JSON
↓
Vue
↓
页面显示连接成功

例如：

Vue 页面：

┌────────────────────────┐
│         邮智办          │
│                        │
│   [ 测试后端连接 ]      │
│                        │
│   当前状态：未连接      │
└────────────────────────┘

点击后，FastAPI 返回：

{
  "success": true,
  "message": "邮智办后端运行正常"
}

前端显示：

✅ 邮智办后端运行正常

这就代表前后端第一次联调成功。