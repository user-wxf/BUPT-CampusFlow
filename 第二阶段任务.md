## 第二阶段目标：

完成“缓考申请”场景下的第一个可完整演示 MVP。

要求系统能够从浏览器端完成一条真实业务闭环：

用户注册 / 登录  
→ 填写并保存学生画像  
→ 输入缓考相关问题  
→ 前端调用 B 后端  
→ B 读取用户画像和 SQLite 事务数据  
→ B 调用 C 的 AI/RAG 服务  
→ C 根据用户画像进行 Metadata Filtering  
→ C 检索缓考知识库  
→ C 结合 B 提供的结构化事务信息生成结果  
→ B 返回前端  
→ 前端展示办理回答、材料、步骤、来源和提示信息  
→ 查询结果保存到 History  
→ 用户可将办理事项加入 Todo

本阶段完成后，项目应从“各模块基本可运行”进入“完整业务流程可以现场演示”的状态。

建议版本：

`v0.4 —— 单事务完整 MVP 版`

---

# 一、成员 A：前端完整业务闭环

## 要做什么

A 负责将目前的基础 Vue 页面扩展为一个可完整操作的学生端 MVP。

需要完成：
```text
- 注册页面或注册功能；
- 登录页面；
- JWT Token 保存和自动携带；
- 用户画像填写与修改；
- 调用 `/api/profile` 保存和读取画像；
- 智能事务查询页面；
- 用户输入自然语言问题；
- 调用 `POST /api/query`；
- 展示后端返回的：
  - answer
  - plans
  - materials
  - steps
  - sources
  - warnings
- Todo 页面或最小 Todo 功能；
- 支持将查询结果中的办理事项加入待办；
- History 页面或最小历史记录功能；
- 支持查看历史查询结果；
- 对接口请求增加最基本的：
  - loading
  - error
  - empty state
- 完成 A → B → C → B → A 的浏览器端完整联调。


前端不要求最终视觉效果，但必须达到：

- 能操作；
- 能展示；
- 能处理基本异常；
- 能支撑课程 Demo。

## 不做什么

本阶段暂不要求：

- 高级动画；
- 复杂视觉特效；
- 完整响应式设计；
- 大量页面美化；
- 完整管理员端视觉设计；
- 多事务复杂导航；
- 用户个性化推荐系统。

优先级：

功能完整 > 联调成功 > 页面美观。

## 交付物

- 可正常启动的 Vue 前端；
- 注册 / 登录功能；
- 用户画像页面；
- 智能查询页面；
- 查询结果展示；
- Todo 最小功能；
- History 最小功能；
- 与 B 后端真实联调成功的前端代码；
- 一条可以现场演示的“缓考申请”完整浏览器流程。

```

# 二、成员 B：后端集成与结构化事务完善

## 要做什么

B 当前后端基础能力已经较完整，因此第二阶段不再以“继续增加大量新接口”为主，而是负责把现有能力整理成稳定的业务中枢。

需要完成：
```text
- 保持现有认证、画像、查询、Todo、History 等接口稳定；
- 修复 A 联调过程中发现的真实接口问题；
- 保证 JWT 登录流程稳定；
- 保证 Profile 能被正确保存和读取；
- 保证 `/api/query` 能稳定调用 C；
- 保证查询结果正确保存到 query_history；
- 准备至少一条正式的“缓考申请” Affair 数据；
- Affair 至少包含：
  - title
  - description
  - materials
  - steps
  - sources
  - eligibility
- 对确有可靠来源的情况下再填写：
  - location
  - room
  - contact
  - office_hours
- 保证 B 传给 C 的 affairs 数据结构稳定；
- 保证 C 返回的数据能够通过 QueryResult 校验；
- 保证 Todo 和 History 能被前端正常调用；
- 配合 A、C 完成完整链路联调；
- 增加必要的集成测试，重点覆盖完整查询流程。

B 在本阶段的核心角色：

从“继续开发更多后端功能”转为“系统集成中心”。

## 不做什么

本阶段暂不要求：

- 新增大量数据库表；
- 大规模重构数据库；
- 新增大量与当前 MVP 无关的 API；
- 复杂权限体系；
- 密码找回；
- Token 黑名单；
- 生产级限流；
- 完整数据库迁移系统；
- 大规模事务数据录入。

如果不是为了“缓考申请完整闭环”所必需，不优先开发。

## 交付物

- 稳定可运行的 FastAPI 后端；
- 注册 / 登录 / JWT；
- Profile；
- Affair；
- Query；
- Todo；
- History；
- 至少一条正式“缓考申请”结构化事务数据；
- B → C 稳定 HTTP 调用；
- A → B 接口稳定联调；
- A → B → C 全链路集成测试结果。

```

# 三、成员 C：从基础检索升级为第一版真实 RAG

## 要做什么

C 当前已经完成：

- 文档整理；
- Chunk；
- Metadata；
- 基础关键词检索；
- `/generate`；
- B → C 联调。

第二阶段需要在保留现有接口稳定的前提下，把 C 从“基础关键词检索服务”升级为“第一版真实 RAG 服务”。

需要完成：
```text
- 保持 `POST /generate` 接口格式不变；
- 保持 B → C 请求格式不变；
- 保持 C → B 返回格式不变；
- 建立 Embedding 流程；
- 将现有缓考 Chunk 向量化；
- 建立轻量 Vector DB；
- 实现向量语义检索；
- 保留 Metadata Filtering；
- 实现：
  - Metadata Filtering
  - Vector Search
  - Top-K Retrieval
- 构建第一版 Context Builder；
- 将检索到的 Chunk 和 B 提供的 affairs 组织成统一上下文；
- 接入 DeepSeek API；
- 设计第一版 Prompt；
- 要求模型严格输出结构化 JSON；
- 模型不得编造：
  - location
  - room
  - contact
  - office_hours
- 以上精确事实优先来自 B 的 affairs；
- 保留真实 Source；
- 对返回结果进行最基本的结构校验；
- 增加少量 RAG 测试问题；
- 比较升级前关键词检索和升级后向量检索的效果；
- 完成 B → C 新版 RAG 联调。

推荐流程：

Chunk  
→ Embedding  
→ Vector DB  
→ Metadata Filtering  
→ Vector Search  
→ Context Builder  
→ DeepSeek  
→ JSON Validation

本阶段知识库仍然以“缓考申请”为主，不要求扩展大量事务。

## 不做什么

本阶段暂不要求：

- Agent；
- 多 Agent；
- 自动网页爬取；
- 自动办理事务；
- 自动登录学校教务系统；
- 大规模知识库；
- 大量事务并行支持；
- 复杂知识图谱；
- Fine-tuning；
- 自训练模型；
- 复杂 Reranker；
- 生产级向量检索架构。

目标是：

先把一个事务的 RAG 做真实、做稳定、做可演示。

## 交付物

- Embedding 构建代码；
- Vector DB；
- 向量检索模块；
- Metadata Filtering；
- Context Builder；
- DeepSeek API 调用；
- Prompt；
- 结构化 JSON 输出；
- Source 追溯；
- 基础结果校验；
- 缓考问题测试结果；
- B → C 真实 RAG 联调成功结果。
```

# 四、三人共同任务

## 要做什么
```text
三人共同完成一次真实端到端联调。

必须从浏览器开始，而不是 Swagger。

完整流程：

用户打开网页  
→ 注册 / 登录  
→ 保存画像  
→ 输入缓考问题  
→ A 调 B  
→ B 查 SQLite  
→ B 调 C  
→ C 做真实 RAG  
→ C 调 DeepSeek  
→ C 返回结构化结果  
→ B 保存 History  
→ B 返回 A  
→ A 展示结果  
→ 用户添加 Todo  
→ 用户查看 History

至少准备 5 条真实测试问题：

- 我生病住院了，可以申请缓考吗？
- 缓考应该什么时候申请？
- 缓考需要什么材料？
- 我没有及时申请缓考怎么办？
- 缓考在哪里操作？

至少测试：

- 正常流程；
- 无相关信息；
- 用户画像不匹配；
- C 服务不可用；
- 返回数据缺失；
- 未登录访问接口。

## 不做什么

本阶段不要因为追求“功能多”而同时扩展大量新事务。

不要求：

- 成绩复核；
- 证明办理；
- 奖学金；
- 请假；
- 转专业；

等多个事务同时做到完整。

如果时间允许，可以提前准备，但不能影响“缓考申请”完整闭环。

## 交付物

最终必须交付一个可以现场直接运行的项目：

`Vue + FastAPI + SQLite + RAG + DeepSeek`

并实现：

`浏览器 → 前端 → 后端 → 数据库 / RAG → 大模型 → 后端 → 前端`

完整闭环。
```

# 五、第二阶段完成标准

只有满足以下条件，第二阶段才算完成：
```
- A → B：PASS
- B → C：PASS
- A → B → C：PASS
- 用户可以从浏览器注册 / 登录；
- 用户可以保存画像；
- 用户可以输入缓考问题；
- B 可以读取 Profile；
- B 可以读取 Affair；
- B 可以调用 C；
- C 可以进行 Metadata Filtering；
- C 可以进行向量检索；
- C 可以调用 DeepSeek；
- C 可以返回真实 Source；
- B 可以保存 History；
- A 可以展示 answer / plans / sources / warnings；
- 用户可以创建 Todo；
- History 可以被重新查看；
- 整个流程不依赖 Swagger 手工操作。

```

# 六、阶段结束后的项目状态

第二阶段完成后，项目建议定义为：

`v0.4 —— 单事务完整 MVP 版`

版本定义：

邮智办已经完成第一个真实校园事务“缓考申请”的完整业务闭环。学生可以通过 Web 前端完成登录、画像填写和自然语言事务咨询，后端能够结合 SQLite 结构化事务数据与 AI/RAG 服务，利用 Metadata Filtering、向量检索和 DeepSeek 生成带真实来源的结构化办理方案，并支持查询历史和待办管理。

当前版本重点证明：

“整个系统已经真实跑通。”

下一阶段再重点解决：

- 更多事务；
- 更多真实资料；
- RAG 准确率优化；
- UI 美化；
- 管理员端完善；
- Demo 体验优化。
